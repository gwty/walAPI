QUnit.test( "Page loads", function( assert ) {
     url = "index.html";
    assert.ok( 1 == "1", "Passed!" );
}); 

// Write unit tests here. I think this application is too simple for unit tests. I may have to learn more about it.
